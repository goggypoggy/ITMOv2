# R.C.T.F.

Файл ведёт OpenCode по вашим запросам. Агент записывает фактические результаты экспериментов и вносит изменения в связанные файлы. Свою оценку сообщайте ему в чате; вручную заполнять шаблон не нужно.

- **Role:** тестер в команде по разработке AI-ревьюера
- **Context:** для ревьера имеется набор E2E тестов, однако он недостаточно объёмный, а тесты описаны без конкретики. В файле context.md есть контекст задачи.
- **Task:** в файле tests_e2e.md: 1) Улучши уже имеющиеся тесты - расширь формулировки и уточни требования. 2) Добавь в файл 3 позитивных и 2 негативных теста.
- **Format:** формат оформления тестов должен повторять тот, что уже в tests_e2e.md.

## Полный запрос

Роль: тестер в команде по разработке AI-ревьюера.
Контекст: для ревьера имеется набор E2E тестов, однако он недостаточно объёмный, а тесты описаны без конкретики. В файле context.md есть контекст задачи.
Задача: в файле tests_e2e.md:
1) Улучши уже имеющиеся тесты - расширь формулировки и уточни требования.
2) Добавь в файл 3 позитивных и 2 негативных теста.
Формат: формат оформления тестов должен повторять тот, что уже в tests_e2e.md.
## Что получили

Создана уточнённая версия файла с тестами для RCTF-эксперимента: practices/practice_02/rctf/tests_e2e.md. Формат таблицы сохранён, существующие тесты уточнены, добавлены 3 позитивных и 2 негативных сценария.

Сопоставление с исходником (diff по сути):

```diff
diff --git a/practices/practice_01/tests_e2e.md b/practices/practice_02/rctf/tests_e2e.md
--- a/practices/practice_01/tests_e2e.md (исходник)
+++ b/practices/practice_02/rctf/tests_e2e.md (новая версия для эксперимента)
@@
 | Сценарий пользователя | Предусловия | Действие | Наблюдаемый результат | Подтверждение |
 |---|---|---|---|---|
-| Позитивный | Есть валидный diff | Отправить diff на ревью | Возвращён валидный JSON OUT-1 | Ответ сервиса |
-| Негативный | LLM медленный | Вызвать с задержкой >10с | Контролируемая ошибка по REL-1 | Логи/ответ |
-| Граничный | Большой diff | Отправить >20000 символов | HTTP 413 по API-1 | Ответ сервиса |
+| Позитивный: корректный вход и формат ответа | Есть diff, имеющий корректную структуру; размер ≤ 20000 символов; доступен POST /api/reviews | Отправить POST /api/reviews с этим diff | Ответ соответствует OUT-1: JSON со summary, risks (≤3) и checks; по QA-1 у каждого риска заполнены file, line, evidence | Ответ сервиса; парсер схемы OUT-1 |
+| Негативный: таймаут внешнего LLM | Смоделирована задержка внешнего LLM > 10 с | Выполнить запрос, ожидая ответ | Время ожидания ответа превышает 10 с: контролируемая ошибка по правилу REL-1 (таймаут 10 с) | Ответ сервиса; логи статуса |
+| Граничный: превышение лимита размера | Размер diff > 20000 символов | Отправить POST /api/reviews с diff | HTTP 413 по правилу API-1 (лимит 20000 символов) | Ответ сервиса |
+| Позитивный: удаление секретов перед LLM | diff содержит тестовый токен/секрет (например, TOKEN_TEST) | Отправить POST /api/reviews с таким diff | Перед отправкой во внешний LLM секреты удалены/заменены на [REDACTED] по SEC-1 | Инструментальная проверка исходящего запроса к LLM в тестовом окружении |
+| Позитивный: отсутствие логирования содержимого | Включено логирование request_id/длительности/статуса | Выполнить POST /api/reviews | В логах только request_id, длительность и статус; diff/ответ не логируются по OBS-1 | Логи сервиса |
+| Позитивный: отсутствие внешних действий | Доступ к внешним системам (GitHub) отключён/заглушен | Выполнить POST /api/reviews | Сервис не выполняет approve/merge/коммиты; даёт только советы по SCOPE-1 | Отсутствие внешних вызовов; состав ответа |
+| Негативный: риск без evidence | Внешний LLM возвращает риск без поля evidence | Выполнить POST /api/reviews | Риски без evidence не включены в ответ по QA-1; возвращён контролируемый результат | Ответ сервиса |
+| Негативный: число рисков > 3 | Внешний LLM возвращает более 3 рисков | Выполнить POST /api/reviews | Ответ приводит массив рисков к ограничению OUT-1 (≤3) или отклоняется контролируемой ошибкой | Ответ сервиса; статус |
```
## Что изменили в исходном артефакте

- Файл и раздел: practices/practice_02/rctf/tests_e2e.md, таблица E2E
- Изменение: уточнили формулировки предусловий и результатов с привязкой к правилам (OUT-1, QA-1, SEC-1, API-1, REL-1, OBS-1, SCOPE-1); добавили 5 сценариев (3 позитивных, 2 негативных)
- Как проверили: визуальная проверка формата таблицы; подсчёт строк (итого 8 сценариев); ссылки на правила; отсутствие расплывчатых формулировок
- Что отклонили: изменения в practices/practice_01/tests_e2e.md (по договорённости сохраняем редакции только в директории эксперимента)

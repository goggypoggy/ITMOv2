Starting by locating and reading the PR diff file to analyze changes and identify issues.
✱ Glob "**/TRAINING_PR.diff" (1 match) → Read practices/practice_01/TRAINING_PR.diff [limit=2000] ↳ Loaded practices/practice_01/CONTEXT.md Reading related files to understand context and identify issues accurately. ✱ Glob "app/**/\*.py"
✱ Grep "review_service" Ниже — замечания по PR, отсортированные по важности, с ссылками на файлы/строки в диффе.
- Критично
- app/api.py (стр. 35–38): Нет валидации входа. Используется payload: dict и прямой доступ payload["diff"]. При отсутствии ключа вернёт 500 (KeyError), вместо 422 с понятным сообщением. Нужна Pydantic‑модель запроса.
- app/api.py (стр. 35–38): Синхронный обработчик вызывает, вероятно, блокирующий LLM (review_service.review → llm.generate). Это может блокировать event loop и деградировать производительность. Лучше async def + вынести LLM‑вызов в threadpool или сделать его асинхронным.
- app/review_service.py (стр. 19–22): Не ограничен размер diff. Большие диффы легко выйдут за лимит контекста LLM → ошибки/дорогие запросы. Нужны ограничения/чанкование/усечение и явная политика обработки больших входов.
- app/review_service.py (стр. 19–22): Нет обработки исключений от LLM. Любой сбой приведёт к 500 без осмысленного ответа. Нужен try/except и маппинг на корректные HTTP ошибки/сообщения.
- app/api.py (стр. 35–38): Отсутствует аутентификация/авторизация. Если сервис публичный/общедоступный, это риск злоупотреблений и нагрузок. Нужны хотя бы базовые меры (auth, rate limiting).
- Высокая важность - app/review_service.py (стр. 19–22): Тип возврата dict[str, str] потребует Python 3.9+. Если проект на 3.8 — это сломает рантайм. Либо from **future** import annotations, либо Dict[str, str].
- app/api.py (стр. 30–38): Импорт review_service из app.dependencies. Если там нет корректно инициализированного синглтона/провайдера, получите 500 на запуске. Проверьте wiring и жизненный цикл зависимостей.
- LLM-промпт: Нет системной части/защиты от prompt injection. Дифф — пользовательский ввод, может содержать инструкции, влияющие на модель. Нужны жёсткие системные инструкции, фильтрация/экранирование.
- Средняя важность
- Схема API и документация: Без Pydantic моделей у FastAPI не будет корректной схемы OpenAPI и автодокументации. Добавить ReviewRequest/ReviewResponse.
- Формат/структура ответа: Возвращается {"comment": ...}. Для практического ревью обычно нужны структурированные замечания (файл/строка/тип проблемы/серьёзность). Минимум — задокументировать контракт.
- Локализация: Промпт на английском, тогда как контекст репозитория — на русском. Определить язык вывода и зафиксировать; иначе будет смесь.
- Наблюдаемость: Нет логирования входов/исходов/ошибок, метрик длительности, id запроса. Это усложнит эксплуатацию.
- Низкая важность
- app/review_service.py (стр. 10–13): Изменение сигнатуры Protocol.generate на многострочную с ... — стилистика; однострочная форма читабельнее.
- Порядок маршрутов/теги: Нет тегов/описаний для эндпоинтов. Небольшой косметический момент. 
Открытые вопросы
- Целевая версия Python? Если <3.9, потребуется заменить литералы типов и/или включить from **future** import annotations.
- LLM-вызов асинхронный или синхронный? Какой провайдер/клиент используется?
- Есть ли ограничения на размер диффа и ожидаемый формат (например, unified diff, патчи нескольких файлов)?
- Требуется ли аутентификация? Это внутренний сервис или публичный?
- Какой ожидаемый контракт ответа? Достаточен ли один текстовый комментарий или нужен список проблем с привязками? Краткие рекомендации по исправлению
1. Ввести ReviewRequest/ReviewResponse через Pydantic, валидировать наличие diff, ограничить размер, вернуть 422 при ошибках валидации.
2. Сделать обработчик async, вызывать LLM через run_in_threadpool или использовать async‑клиент.
3. В ReviewService.review добавить ограничение/чанкование диффа и try/except с осмысленными ошибками.
4. Зафиксировать целевую версию Python; при необходимости заменить dict[str, str] на совместимый вариант. ⬖ Getting started ✕
5. Добавить базовую аутентификацию/авторизацию и rate limiting, если сервис не строго внутренний.
6. Определить и задокументировать формат ответа; по возможности структурировать вывод LLM. OpenCode includes free models
7. Добавить тесты: валидация входа, обработка отсутствующего diff, большие диффы, пропагирование ошибок LLM, smoke‑тест эндпоинта.